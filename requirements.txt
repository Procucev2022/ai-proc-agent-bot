# AI Procurement Agent Dependencies
# Core web framework and API
fastapi
uvicorn
gunicorn
python-dateutil
pytz
jinja2
# Database and ORM
sqlalchemy
PyMySQL
alembic

# Data validation and serialization
pydantic

# OpenAI integration
openai

# HTTP requests and API clients
requests
httpx
aiohttp

# Environment and configuration
python-dotenv

# Caching and session management
redis
slowapi

# Testing framework (pinned to keep the coverage gate reproducible)
pytest==8.4.1
pytest-asyncio==1.1.0
pytest-timeout==2.4.0
coverage==7.10.6

# Excel processing
pandas
openpyxl
xlrd

# Multi-process-safe log rotation (10 gunicorn workers share app.log)
concurrent-log-handler

# Vector database for auto-categorization
chromadb
sentence-transformers

# Background job processing for seller recommendation system
celery
redis

# Geographic calculations for seller location matching
geopy
pgeocode


# Static analysis (pinned: the PR quality gate compares finding counts against
# quality-baseline.json, and those counts are only reproducible per tool version)
black==26.5.1
flake8==7.3.0
isort==8.0.1
mypy==2.3.0

python-multipart