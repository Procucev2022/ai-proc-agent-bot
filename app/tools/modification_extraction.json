{
    "type": "function",
    "name": "extract_modification_values",
    "description": "Extract ONLY the new modification values from user message. Do NOT extract existing values or copy over current data. Focus only on what the user explicitly wants to change.",
    "parameters": {
        "type": "object",
        "properties": {
            "modifications": {
                "type": "array",
                "description": "Array of modification values extracted from the message - ONLY new values provided by user",
                "items": {
                    "type": "object",
                    "properties": {
                        "field_type": {
                            "type": "string",
                            "enum": ["delivery_location", "quantity", "delivery_date", "description", "division", "brand", "project_desc", "unit_of_measures", "remarks"],
                            "description": "Type of field being modified"
                        },
                        "new_project_desc": {
                            "type": ["string", "null"],
                            "description": "NEW project description - only if user provided a new one"
                        },
                        "new_description": {
                            "type": ["string", "null"],
                            "description": "NEW item description - only if user provided a new one"
                        },
                        "new_quantity": {
                            "type": ["number", "null"],
                            "description": "NEW quantity - only if user provided a new quantity"
                        },
                        "new_unit_of_measures": {
                            "type": ["string", "null"],
                            "description": "NEW unit of measurement - only if user provided a new one"
                        },
                        "new_delivery_date": {
                            "type": ["string", "null"],
                            "description": "NEW delivery date - only if user provided a new date"
                        },
                        "new_division": {
                            "type": ["string", "null"],
                            "description": "NEW division - only if user provided a new division"
                        },
                        "new_brand": {
                            "type": ["string", "null"],
                            "description": "NEW preferred brand - only if user provided a new brand"
                        },
                        "new_state": {
                            "type": ["string", "null"],
                            "description": "NEW delivery state - only if user provided a new state"
                        },
                        "new_city": {
                            "type": ["string", "null"],
                            "description": "NEW delivery city - only if user provided a new city"
                        },
                        "new_pincode": {
                            "type": ["string", "null"],
                            "description": "NEW delivery pincode - only if user provided a new pincode"
                        },
                        "new_remarks": {
                            "type": ["string", "null"],
                            "description": "NEW remarks - only if user provided new remarks"
                        }
                    },
                    "required": ["field_type", "new_project_desc", "new_description", "new_quantity", "new_unit_of_measures", "new_delivery_date", "new_division", "new_brand", "new_state", "new_city", "new_pincode", "new_remarks"],
                    "additionalProperties": false
                }
            },
            "has_new_values": {
                "type": "boolean",
                "description": "True if user provided actual new values, False if only expressed modification intent without new values"
            },
            "modification_intent": {
                "type": ["string", "null"],
                "description": "What the user wants to modify (e.g., 'delivery address', 'quantity') even if no new values provided"
            },
            "confidence": {
                "type": "number",
                "minimum": 0,
                "maximum": 100,
                "description": "Confidence in the modification extraction accuracy"
            }
        },
        "required": ["modifications", "has_new_values", "modification_intent", "confidence"],
        "additionalProperties": false
    },
    "strict": true
}