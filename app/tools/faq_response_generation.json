{
  "type": "function",
  "name": "generate_faq_response",
  "description": "Generate response to user question using FAQ content",
  "parameters": {
    "type": "object",
    "properties": {
      "answer": {
        "type": "string",
        "description": "The answer to the user's question from FAQ content, or fallback message if not found"
      },
      "found_in_faq": {
        "type": "boolean",
        "description": "Whether the answer was found in the FAQ content"
      },
      "confidence": {
        "type": "number",
        "minimum": 0,
        "maximum": 100,
        "description": "Confidence level in the answer (0-100)"
      },
      "related_topics": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "List of related FAQ topics that might be helpful"
      }
    },
    "required": ["answer", "found_in_faq", "confidence"]
  }
}
