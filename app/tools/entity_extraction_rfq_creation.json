{
    "type": "function",
    "name": "extract_entities",
    "description": "Extract structured entities from user message for RFQ creation workflow. Can handle multiple products in a single message. Delivery location and date are GLOBAL fields that apply to all products. Focus only on entity extraction - completeness checking is handled by the data model.",
    "parameters": {
        "type": "object",
        "properties": {
            "products": {
                "type": "array",
                "description": "Array of product entities extracted from the message. Each product contains item-specific fields only.",
                "items": {
                    "type": "object",
                    "properties": {
                        "description": {
                            "type": "string",
                            "description": "Item description. Use empty string if not provided."
                        },
                        "quantity": {
                            "type": ["number", "null"],
                            "description": "Required quantity - numeric value ONLY. Do NOT confuse specifications with quantity. If uncertain, use null. Examples: '100 laptops' -> 100, 'Motors 1000KV' -> null (1000KV is spec, not quantity), '25 meters cable, 100 qty' -> 100."
                        },
                        "unitofMeasures": {
                            "type": "string",
                            "description": "Unit of measurement extracted from context: pcs, kg, meters, liters, boxes, sets, etc. Extract from user's message if explicitly mentioned (e.g., '25 meters cables' -> 'meters', '50kg rice' -> 'kg'). Use empty string ONLY if no unit is mentioned AND cannot be inferred. DO NOT default to 'pcs' unless explicitly stated or clearly implied for countable items."

                        },
                        "brand": {
                            "type": "string",
                            "description": "Preferred brand name for this item. Use empty string if not provided."
                        },
                        "remarks": {
                            "type": "string",
                            "description": "Item-specific remarks or requirements. Use empty string if not provided."
                        }
                    },
                    "required": ["description", "quantity", "unitofMeasures", "brand", "remarks"],
                    "additionalProperties": false
                }
            },
            "deliveryDate": {
                "type": "string",
                "description": "Required delivery date (applies to all products). Use empty string if not provided."
            },
            "state": {
                "type": "string",
                "description": "Delivery state for all products (e.g., Karnataka, Maharashtra, etc.). Use empty string if not provided."
            },
            "city": {
                "type": "string",
                "description": "Delivery city for all products (e.g., Bangalore, Mumbai, etc.). Use empty string if not provided."
            },
            "pincode": {
                "type": "string",
                "description": "Delivery pincode for all products (6-digit postal code). Use empty string if not provided."
            },
            "confidence": {
                "type": "number",
                "minimum": 0,
                "maximum": 100,
                "description": "Confidence in the entity extraction accuracy"
            }
        },
        "required": ["products", "deliveryDate", "state", "city", "pincode", "confidence"],
        "additionalProperties": false
    },
    "strict": true
}