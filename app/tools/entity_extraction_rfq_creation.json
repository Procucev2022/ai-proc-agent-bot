{
    "type": "function",
    "name": "extract_entities",
    "description": "Extract structured entities from user message for RFQ creation workflow. Can handle multiple products in a single message. Delivery location and date are GLOBAL fields that apply to all products. Focus only on entity extraction - completeness checking is handled by the data model.",
    "parameters": {
        "type": "object",
        "properties": {
            "products": {
                "type": "array",
                "description": "Array of product entities extracted from the message. Each product contains item-specific fields only.",
                "items": {
                    "type": "object",
                    "properties": {
                        "description": {
                            "type": ["string", "null"],
                            "description": "Item description"
                        },
                        "quantity": {
                            "type": ["number", "null"],
                            "description": "Required quantity - numeric value"
                        },
                        "unitofMeasures": {
                            "type": ["string", "null"],
                            "description": "Unit of measurement: pcs, kg, meters, etc."
                        },
                        "brand": {
                            "type": ["string", "null"],
                            "description": "Preferred brand name for this item"
                        },
                        "remarks": {
                            "type": ["string", "null"],
                            "description": "Item-specific remarks or requirements"
                        }
                    },
                    "required": ["description", "quantity", "unitofMeasures", "brand", "remarks"],
                    "additionalProperties": false
                }
            },
            "deliveryDate": {
                "type": ["string", "null"],
                "description": "Required delivery date (applies to all products)"
            },
            "state": {
                "type": ["string", "null"],
                "description": "Delivery state for all products (e.g., Karnataka, Maharashtra, etc.)"
            },
            "city": {
                "type": ["string", "null"],
                "description": "Delivery city for all products (e.g., Bangalore, Mumbai, etc.)"
            },
            "pincode": {
                "type": ["string", "null"],
                "description": "Delivery pincode for all products (6-digit postal code)"
            },
            "confidence": {
                "type": "number",
                "minimum": 0,
                "maximum": 100,
                "description": "Confidence in the entity extraction accuracy"
            }
        },
        "required": ["products", "deliveryDate", "state", "city", "pincode", "confidence"],
        "additionalProperties": false
    },
    "strict": true
}