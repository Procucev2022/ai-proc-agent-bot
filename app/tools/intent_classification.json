{
    "type": "function",
    "name": "classify_intent",
    "description": "Classify user intent for procurement agent routing with confidence scoring and multi-intent analysis",
    "parameters": {
        "type": "object",
        "properties": {
            "intent": {
                "type": "string",
                "enum": ["buy_something", "general_inquiry", "ambiguous"],
                "description": "The primary classified intent category based on user message analysis"
            },
            "confidence": {
                "type": "number",
                "minimum": 0,
                "maximum": 100,
                "description": "Confidence score as percentage (0-100) for the primary classification"
            },
            "all_intent_scores": {
                "type": "object",
                "properties": {
                    "buy_something": {
                        "type": "number",
                        "minimum": 0,
                        "maximum": 100
                    },
                    "general_inquiry": {
                        "type": "number",
                        "minimum": 0,
                        "maximum": 100
                    }
                },
                "required": ["buy_something", "general_inquiry"],
                "additionalProperties": false,
                "description": "Confidence scores for all intent categories"
            },
            "reasoning": {
                "type": "string",
                "description": "Brief explanation of why this intent was chosen and key indicators"
            },
            "suggested_clarification": {
                "type": ["string", "null"],
                "description": "If confidence is low or intents are similar, suggest what clarification question to ask"
            }
        },
        "required": ["intent", "confidence", "all_intent_scores", "reasoning", "suggested_clarification"],
        "additionalProperties": false
    },
    "strict": true
}