{
  "type": "function",
  "name": "analyze_user_selection",
  "description": "Analyze user input to determine which profile option they are selecting from a list of numbered options",
  "parameters": {
    "type": "object",
    "properties": {
      "selected_option": {
        "type": ["integer", "null"],
        "description": "The option number the user selected (1, 2, 3, etc.) or null if unclear"
      },
      "confidence": {
        "type": "number",
        "minimum": 0.0,
        "maximum": 1.0,
        "description": "Confidence level in the selection (0.0-1.0)"
      },
      "reasoning": {
        "type": "string",
        "description": "Explanation of why this option was selected and the analysis process"
      },
      "alternative_matches": {
        "type": "array",
        "items": {"type": "integer"},
        "description": "Other possible option numbers that could match the user input"
      },
      "requires_clarification": {
        "type": "boolean",
        "description": "Whether the input is too ambiguous and requires user clarification"
      }
    },
    "required": ["selected_option", "confidence", "reasoning", "alternative_matches", "requires_clarification"]
  }
}